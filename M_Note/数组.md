---
date created: 2026-09-06 16:46
tags:
  - matlab
---

在 MATLAB 的所有数据类型都是按照数组存储和运算的，数组的内容可以是任意常见的数据类型。

# 1 创建数组

1、数组创建使用 `[]`，`,`，`;` 和空格，这是最基础的创建数组的方式

```m
array = [1 2 3; 4 5 6]
```

2、常用函数

- `linspace`
- `'`
- `logspace`
- `magic`

# 2 数组属性

## 2.1 数组结构类

数组结构指的是数组的元素排列方式

- `isempty`
- `isscalar`
- `isvector`
- `issparse`

## 2.2 数组大小类

这是最常用的属性，指的是数组在每个方向上的元素个数

- `size`
- `length`

在 MATLAB 中，`length(A)` 的定义是：返回 `size(A)` 中**最大的那个维度长度**，即 `max(size(A))`。

- `numel`

## 2.3 数组维度

- `ndims`

其实 `ndims(A)` 返回的是 `length(size(A))`，即 `size` 结果的长度。而 MATLAB 中**所有数组都至少是二维的**（没有真正意义上的一维数组）。`linspace(1,10,10)` 生成的是一个 **1×10 的行向量**，它的 `size` 是 `[1 10]`，长度为 2，所以 `ndims` 返回 2。

## 2.4 数据类型

- `isnumeric`
- `isfloat`
- `isfloat`
- `ischar`
- `isreal`

# 3 创建特殊数组

## 3.1 0-1 数组

数组的元素全是 0 和 1 构成。

- `zeros(m,n)`
- `zeros(size(A))`
- `ones(m,n)`
- `ones(size(A))`
- `eyes(n)`

## 3.2 对角数组

- `diag`

一般 `diag` 函数接收一个一维行向量数组为输入参数，将此向量的元素逐次排列在所指定的对角线上，其他位置则用 0 填充。

1. `diag(v)`：创建一个对角数组，其主对角线元素依次对应于向量 v 的元素。
2. `diag(v,k)`：创建一个对角数组，其第 k 条对角线元素对应于向量 v 的元素。
   3. 当 k 大于 0 时，表示主对角线向右上角偏离 k 个元素位置的对角线。
   4. 当 k 等于 0 时，则和 `diag(v)`一样。
   5. 当 k 小于 0 时，表示主对角线向左下角偏离 k 个元素位置的对角线。
6. diag 函数也可以接受普通二维数组形式的输入参数，此时就不是创建对角数组了，而是从已知数组中提取对角元素组成一个一维数组
   7. `diag(X)`提取二维数组 X 的主对角线元素组成一维数组。
   8. `diag(X,k)`提取二维数组X的第k条对角线元素组成的一维数组。

## 3.3 随机数组

- `rand(m,n)`
- `randn(m,n)`

## 3.4 魔方数组

# 4 数组操作

## 4.1 保存和装载

save 和 load

## 4.2 索引

### 4.2.1 索引形式

- 双下标索引
- 单下标索引
  - 对于 m 行 n 列的数组 A，`A(x,y)` 实际上对应于 `A((y-1)*m+x)`
  - 按照列优先的顺序排列
- 逻辑索引
### 4.2.2 索引转换
+ 双下标索引->单下标索引：`subind（sz,i,j）`
+ 单下标索引->双下标索引：`ind2sub(sz,idx)`
### 4.2.3 数组扩展和裁剪
+ 变量编辑器
+ `cat` 函数
	+ `cat(dim,x,y,...)`
	+ `horzcat(x,y,...)`
	+ `vertcat(x,y,...)`
+ 块操作
	+ `repmat(A,m,n)`：把 A 整体当做一个元素，按照 m 行 n 列复制
	+ `blkdiag(A,B,...)`：把 A，B 等数组当做对角线元素创建新数组，其他元素使用 0 填充
		+ 不要求同维度
	+ `kron(A,B)`：就是把矩阵 A 中的每一个元素，都乘上整个矩阵 B，然后按照 A 的结构拼起来。
### 4.2.4 索引扩展
之前说的是使用索引去寻找数组的元素，所以索引的范围必须在原来数组的范围里。现在是使用索引对数组进行扩展，自然就没有这个限制了，对于扩展后的其他位置上的元素都使用 `0` 进行填充
### 4.2.5 通过冒号裁剪数组
### 4.2.6 数组元素删除
通过删除数组的部分元素，也可以实现对数组的裁剪。删除数组的元素很简单，就是对特定位置的元素赋值为 `[]`。删除的时候必须是完整的行或者完整的列，不可以是块或者单个元素。
同时删除数组的元素是直接对原数组进行操作的，所以需要考虑是否在操作之前备份原数组。


## 4.3 数组形状的改变
通常包括数组的转置，平移和旋转，以及数组尺寸的重新调整
## 4.4 数组转置
通过转置操作符 `'` 即可实现转置，但是需要注意：
1. 对于有复数的数组，使用转置操作符的时候会把复数元素转换成共轭复数；
2. 如果不想这样则使用 `.'` 操作符。
除了操作符也有对应的函数
+ `ctanspose`--> `'`
+ `transpose`--> `.'`
