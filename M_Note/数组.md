在 MATLAB 的所有数据类型都是按照数组存储和运算的，数组的内容可以是任意常见的数据类型。

# 创建数组
1、数组创建使用 `[]`，`,`，`;` 和空格，这是最基础的创建数组的方式
```m
array = [1 2 3; 4 5 6]
```
2、常用函数
+ `linspace`
+ `'`
+ `logspace`
+ `magic`
# 数组属性
+ `isempty`
+ `isscalar`
+ `isvector`
+ `issparse`
